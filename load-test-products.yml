
config:
  target: "http://localhost:3000"
  phases:
    - duration: 60
      arrivalRate: 10
      rampTo: 50
      name: Warm up
    - duration: 120
      arrivalRate: 500
      name: Sustained load
scenarios:
  - name: "Get products"
    flow:
      - get:
          url: "/products"
